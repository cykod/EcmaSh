<div class='line'>Register for an account</div>
{{#if error}}
  <div class='line error'>{{error}}</div>
{{/if}}
{{line "username:" username}}
{{#if username}}
  {{#if password}}
    {{line "password:" "•••••"}}
  {{else}}
    {{line "password:" password}}
  {{/if}}
{{/if}}

{{#if password}}
{{line "email:" email}}
<div class='line'>(optional, for account retrieval)</div>
{{/if}}
